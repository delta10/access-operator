---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: postgresaccesses.access.k8s.delta10.nl
spec:
  group: access.k8s.delta10.nl
  names:
    kind: PostgresAccess
    listKind: PostgresAccessList
    plural: postgresaccesses
    singular: postgresaccess
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PostgresAccess is the Schema for the postgresaccesses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PostgresAccess
            properties:
              cleanupPolicy:
                default: Restrict
                description: |-
                  cleanupPolicy specifies how to handle owned objects when dropping a user
                  Cascade: drops all objects owned by the user (destructive)
                  Restrict: prevents dropping the user if they own any objects (safe default)
                  Orphan: reassigns owned objects to the current database owner before dropping
                enum:
                - Cascade
                - Restrict
                - Orphan
                type: string
              connection:
                description: |-
                  connection defines how to connect to PostgreSQL
                  Can reference an existing secret or provide connection details directly
                properties:
                  database:
                    description: database is the database name
                    minLength: 1
                    type: string
                  existingSecret:
                    description: |-
                      existingSecret references an existing secret with connection details
                      The secret must contain keys: host, port, dbname, username, password
                      Optionally can include: sslmode
                    type: string
                  host:
                    description: host is the PostgreSQL server hostname
                    minLength: 1
                    type: string
                  password:
                    description: |-
                      password for connecting to the database
                      Can be provided as plain text or as a secret reference
                    properties:
                      secretRef:
                        description: secretRef references a key in a secret
                        properties:
                          key:
                            description: key is the key within the secret
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the secret
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      value:
                        description: value is the plain text value
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: either value or secretRef must be specified
                      rule: has(self.value) || has(self.secretRef)
                  port:
                    description: port is the PostgreSQL server port
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  sslMode:
                    default: require
                    description: |-
                      sslMode specifies the SSL mode for the connection
                      Valid values: disable, allow, prefer, require, verify-ca, verify-full
                      Defaults to "require" for security
                    enum:
                    - disable
                    - allow
                    - prefer
                    - require
                    - verify-ca
                    - verify-full
                    type: string
                  username:
                    description: |-
                      username for connecting to the database
                      Can be provided as plain text or as a secret reference
                    properties:
                      secretRef:
                        description: secretRef references a key in a secret
                        properties:
                          key:
                            description: key is the key within the secret
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the secret
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      value:
                        description: value is the plain text value
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: either value or secretRef must be specified
                      rule: has(self.value) || has(self.secretRef)
                type: object
              generatedSecret:
                description: generatedSecret is the name of the secret where generated
                  credentials will be stored
                minLength: 1
                type: string
              grants:
                description: grants defines the database privileges to grant
                items:
                  description: GrantSpec defines database grants to be applied
                  properties:
                    database:
                      description: database is the name of the database to grant privileges
                        on
                      minLength: 1
                      type: string
                    privileges:
                      description: privileges is the list of privileges to grant
                      items:
                        type: string
                      minItems: 1
                      type: array
                    schema:
                      default: public
                      description: schema is the database schema (defaults to "public")
                      minLength: 1
                      type: string
                  required:
                  - database
                  - privileges
                  type: object
                maxItems: 1
                minItems: 1
                type: array
              username:
                description: |-
                  username is the PostgreSQL username to create
                  If empty, the operator will generate one
                type: string
            required:
            - connection
            - generatedSecret
            - grants
            - username
            type: object
          status:
            description: status defines the observed state of PostgresAccess
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the PostgresAccess resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
